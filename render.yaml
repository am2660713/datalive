services:
  - type: web
    name: datalive
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_ORIGIN
        sync: false
      - key: MANAGER_EMAILS
        sync: false
      - key: MANAGER_INVITE_CODE
        sync: false
